# A set is used to store unique values.
# Sets are written using {}.
# A set does not allow duplicate values.
# You can add or remove items, but you cannot access items by index.

colors = {"Red", "Blue", "Green"}
print(colors)
