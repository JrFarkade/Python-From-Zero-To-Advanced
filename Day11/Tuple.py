# Tuple
# A tuple is used to store multiple values.
# Tuples are written using () and cannot be changed.
# But once created, tuple values cannot be changed (tuple is immutable).

# Example:
numbers = (10, 20, 30)
print(numbers)
